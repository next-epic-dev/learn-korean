# DramaKorean — 그로스 루프 로그

---

## 2026-07-29 11:10 UTC — 루프 8

**라운드2 심사 통과(DELIVERY_OK). 그런데 승인 30분이 지나도록 노출 0이다.
그리고 그 "노출 0"을 판정하려다 나 자신이 먼저 오독했다 — 그 오독을 도구에 못 박았다.**

### 데이터 스냅샷
- 틱톡 오늘(07-29, acct tz UTC-5) DramaKorean 지출 **$0.00 / 캡 $30** — 위반 없음
- **광고그룹 `1872041858734178` = `ADGROUP_STATUS_DELIVERY_OK`** (승인 10:24 UTC / 05:24 acct)
  **광고 `1872041900786353` = `AD_STATUS_DELIVERY_OK`** (승인 10:39 UTC / 05:39 acct)
  → 루프 7이 남긴 유일한 게이트(심사)는 **열렸다**. 반려 아님, 스케줄 연장 불필요
  (`schedule_end_time` 07-30 04:43 acct = 07-30 09:43 UTC, 아직 ~22h 남음)
- 라운드2 실적: **노출 0, 클릭 0, 지출 $0.00** (승인 후 ~30분 경과)
- 루프 7 이후 **실사용 세션 0** (`--since 09:50Z` → `0 raw → 0`)
- 규율 적용: 신규 실사용 세션 0 (<30) → 가격·타겟·훅 **변경 금지**

### 🔑 이번 루프의 발견 — "리포트가 2시간 밀렸다"는 내 판정이 틀렸다
승인 직후 노출 0을 보고, 먼저 **리포트 지연을 의심**했다. 시간별 리포트를 뽑아
다른 광고그룹의 최신 보고 시각을 확인했더니 `04:00 acct`. 지금은 `06:08 acct`.
"리포트가 2시간 밀렸다 → 승인(05:39) 이후 구간은 물리적으로 보고될 수 없다 →
노출 0은 정보가 아니라 사각지대다" — 여기까지 갔다.

**틀렸다. 나는 시간 목록의 `max`가 아니라 `[-1]`(마지막 원소)을 봤다.**
리포트 행 순서는 정렬돼 있지 않다. 제대로 `max`를 잡으면 워터마크는 `06:00 acct`,
즉 **진행 중인 현재 시각까지 보고되고 있다(~0.2h 지연).**

| 광고그룹 | 첫 보고시 | 마지막 보고시 | 노출 |
|---|---|---|---|
| 1871924991153298 (GlowGate) | 00:00 | **06:00** | 885 |
| 1871968197538945 (GlowGate) | 00:00 | 04:00 | 654 |
| **1872041858734178 (라운드2)** | — | — | **행 자체가 없음** |

같은 계정의 다른 광고그룹이 **바로 이 시간(06:00)에 노출을 찍고 있다.**
리포트는 살아있고, 경매도 이 계정에 대해 지금 돌아간다.
**따라서 라운드2의 노출 0은 사각지대가 아니라 진짜 0이다.**

이 오독이 위험한 이유는 방향이 정반대라서다. "사각지대"로 결론냈다면 다음 루프는
"보고만 밀린 것"이라며 몇 시간을 더 기다렸을 것이고, 집행 창(22h)을 그냥 태웠을 것이다.

### 결정과 근거
**액션 = 광고 무손질 + 판독기를 지연 인지형으로 고침(위생 수리).**

라운드2 광고 오브젝트는 **손대지 않았다.** 승인 30분, 그것도 **06:00 acct(미국 중부 새벽
6시) — 하루 중 미국 오디언스가 가장 얇은 시간대**다. 신규 광고그룹은 승인 후 첫 노출까지
램프업 지연이 흔하다. 30분치 0으로 "오디언스가 좁다"를 선언하는 건 루프 1~5가 반복한
바로 그 성급한 오진이다. 지금 타겟을 넓히면 그 결정이 옳았는지 영영 알 수 없게 된다.

대신 **다음 루프의 판단 기준을 미리 못 박는다**(아래 "사전 확약 규칙"). 스레시홀드를
데이터를 보기 전에 정해두는 것이 이 프로젝트가 반복한 사후 합리화를 막는 유일한 방법이다.

한편 이번에 실제로 고칠 게 생겼다: **나를 속인 그 판독 실수 자체.**
워터마크는 매 루프가 돈 쓰는 결정 전에 반드시 보는 숫자인데, 지금까지 아무도 안 봤고,
내가 손으로 뽑자마자 틀렸다. 사람이 매번 손으로 뽑으면 매번 틀릴 수 있다 → 도구에 넣는다.

### 실행 내용 (커밋 1개, `tools/dk_read.py`만 — 페이지·광고 무변경)
**1) `delivery_watermark()` — `--spend`가 매번 자동 출력**
전 광고그룹에서 노출>0인 시간 버킷의 **`max`**를 잡아 계정 현재시각과의 지연을 계산.
`>=1.5h`면 "이 구간의 0은 사각지대다, dk_events를 보라"고 명시 경고. 오늘처럼 ~0.2h면
경고 없이 워터마크만 → **0을 0으로 읽어도 된다는 허가가 자동으로 나온다.**
오늘 실측 출력: `report watermark: 2026-07-29 06:00:00 acct (acct now 06:08) -> ~0.1h behind`

**2) `--recon` — 광고쪽 클릭 vs 페이지쪽 착지 세션 대조**
라운드1은 클릭 266 → 세션 249(93.6%)였다. 이 간격이 갑자기 벌어지면 그건 퍼널 문제가
아니라 **클릭이 페이지에 도달조차 못 하는** 완전히 다른 버그다(리다이렉트/CDN/동의창).
`landed < 0.7*clicks`면 "퍼널 말고 랜딩 URL을 보라"고 경고. 워터마크를 항상 함께 찍어서
**리포트는 밀리고 dk_events는 안 밀리는 비대칭**을 오독하지 않게 했다.
(기본 동작 무변경 — 옵트인 플래그. 실측 통과: `impressions 0 -> clicks 0 vs landed 0`.)

**계측 안전:** `episode1/index.html` **무수정 → 배포 없음.** track()/dk_events 경로는
손대지 않았고, 이번 루프는 라이브 페이지를 **헤드리스로 열지도 않았다**
(루프 7이 경고한 자가 오염을 0으로 유지 — dk_events에 이번 루프가 남긴 행은 없다).

### 사전 확약 규칙 (다음 루프가 데이터 보기 전에 따를 것)
- **승인 후 ~2시간(≈12:40 UTC / 07:40 acct) 시점에도 노출이 여전히 0이면** → 램프업이
  아니라 **경매 진입 실패**로 판정. 액션은 **최소 확장 1회**: `interest_keyword_ids`
  5개(narrow keyword 타겟)를 제거하거나 interest **category**로 완화.
  **`PLACEMENT_TIKTOK` 단독은 유지 — Pangle은 절대 되돌리지 않는다**(라운드1 쓰레기
  트래픽의 유력 용의자). 연령 25-54, 미국도 유지. 한 번에 한 변수만 푼다.
- **노출이 나오기 시작했으면** 확장 금지. 그때부터는 퍼널 판독 국면이다.

### 다음 루프 제안
1. `python3 tools/dk_read.py --spend` — 이제 **워터마크가 같이 찍힌다.**
   $30 초과면 `1872041858734178` 즉시 정지. 0을 읽기 전에 **워터마크 경고 유무를 먼저 볼 것.**
2. 위 **사전 확약 규칙**으로 라운드2 집행 여부 판정 (`/adgroup/get/`+시간별 리포트 `max`).
   - 집행 실패 확정 시, 이건 사용자에게 알릴 가치가 있다 — 집행 창이 07-30 09:43 UTC에
     닫히고, 죽은 광고그룹은 **조용히** 실험 전체를 날린다.
3. 노출이 붙었으면 `python3 tools/dk_read.py --since <첫 노출시각> --recon`.
   판독 순서(유지): 클릭→착지 비율(93.6% 기준선) → `perf`/`first_tap` → `scene_play`→
   `scene_audio_ok`(낮으면 인앱 오디오 차단, 무음 자막 우선 모드) → `leave.sec` 3초 미만
   집중(훅 불일치, `wall`과 혼동 금지) → `offer_view` 도달 후 `checkout_click` 0 (n≥20)이면 $29.
4. 라이브 페이지 헤드리스 검증 시 `?dk_qa=1` + `dk_events` 라우트 차단 — 둘 다 할 것.
5. 규율 유지: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것. 루프당 의미 있는 변수 1개.
6. 가드레일 메모: 캠페인은 `BUDGET_MODE_INFINITE`다. **$30 캡을 지키는 건 광고그룹
   일예산뿐**이므로, 앞으로 DramaKorean 광고그룹을 하나 더 만들면 캡이 즉시 깨진다.
   추가할 거면 기존 $30을 쪼개서 나눠라.

---

## 2026-07-29 10:25 UTC — 루프 7

**라운드2는 아직 심사 중(AUDIT) — 노출 0, 지출 $0. 즉 실사용 신규 트래픽은 0이다.
그리고 그 "0"을 확인하다가, 루프 6이 자기 데이터를 스스로 오염시키고 있었다는 걸 찾았다.**

### 데이터 스냅샷
- 틱톡 오늘(07-29, 계정 tz UTC-5) DramaKorean 지출 **$0.00 / 캡 $30** — 초과 위험 없음
  (같은 계정의 GlowGate 캠페인 1871924997477506이 $3.03 썼지만 다른 제품, 무접촉)
- 광고그룹 `1872041858734178` = `ADGROUP_STATUS_AUDIT`,
  광고 `1872041900786353` = `AD_STATUS_AUDIT` (10:25 UTC 기준, 생성 후 ~40분)
- 캠페인 `1871949785120465`은 `BUDGET_MODE_INFINITE`라 캠페인 단에서 막는 건 없음.
  일예산 $30은 광고그룹에 정상 설정됨
- 루프 6 이후 **실사용 세션 0** (아래 참조)

### 🔑 이번 루프의 발견 — 우리가 우리 데이터에 트래픽을 쏘고 있었다
루프 6 이후 창을 읽으니 `utm_campaign=dramakorean_ep1_r2` 세션이 **2개** 잡혔다.
그런데 광고는 심사 중이고 노출이 0이다. 사람이 도달할 수 없는 URL이다.

원시 행을 보면 정체가 분명하다:

| session | 09:44:45 / 09:44:50 | 이벤트 | 탭 |
|---|---|---|---|
| `ms5wesk52a0cjlox` | page_view · perf · scroll | 3개 | **0** |
| `ms5wevy75zyi9sd9` | page_view · perf · scroll | 3개 | **0** |

**루프 6이 새 랜딩 URL을 헤드리스로 확인한 그 로드들이다.** 매 루프가 라이브 페이지를
헤드리스로 검증하는데, 그 로드가 유료 트래픽과 **완전히 같은 모양으로** dk_events에 적재된다 —
`page_view + perf`만 있고 탭이 없는, **완벽한 가짜 이탈자**.

루프 6은 실제로 여기 걸렸다. 09:13의 세션을 "수정 후 실사용 세션 1개"로 셌는데,
그 세션도 탭이 하나도 없는 같은 모양이다. 그리고 이건 정확히 **6개 루프를 태운 그 착시의
재발**이다 — 루프 1~5는 "249명이 로드해놓고 아무도 재생을 안 눌렀다"는 공포에 시달렸고,
루프 6이 그게 깨진 빌드의 기록이었음을 밝혔다. 이번엔 **우리 자신의 봇**이 같은 모양의
가짜 이탈자를 만들고 있었고, 하필 라운드2 코호트 안에 앉아 있었다.
라운드2는 **처음으로 고쳐진 페이지의 데이터**다. 오염된 채로 받으면 다음 루프가
멀쩡한 퍼널을 훅 문제로 오진하고, 그 오진으로 실제 돈 쓰는 결정을 한다.

### 결정과 근거
실사용 신규 세션 0 → 규율상 큰 변수(가격·타겟·훅) 변경 금지. 백로그 1(지출 확인) 완료,
2(점화)는 루프 6이 이미 완료, 3(퍼널 판독)은 **판독할 트래픽이 아직 없어** 불가.
심사는 우리가 앞당길 수 없다(계정 조작으로 심사를 흔드는 건 오히려 리스크).
그래서 이번 루프의 유일한 변수 변경 = **라운드2 데이터가 도착하기 전에 오염원 차단.**
타이밍이 정확히 지금인 이유: 트래픽이 0인 지금 고쳐야 라운드2가 깨끗한 채로 들어온다.

### 실행 내용 (커밋 `bd5366a` 1개, 되돌리기 쉬움)
**1) `episode1/index.html` — QA/자동화 로드에 person_id 태그**
```
DK_QA = navigator.webdriver === true || ?dk_qa=1
DK_PID = (DK_QA ? 'PREVIEW-QA-' : '') + (?id 또는 'anon')
```
`PREVIEW` 접두사는 **이미 있는 제외 규칙**에 걸린다 — 판독기에 새 규칙을 더할 필요 없이
세션 통째로 빠진다. `navigator.webdriver`가 CDP 구동 브라우저(Playwright/Puppeteer)를 잡고,
`?dk_qa=1`은 그걸 안 세우는 검증용 탈출구. **실사용자는 둘 다 해당 없음 → 그대로 `anon`.**

**2) `tools/dk_read.py` — 루프 6의 r2 검증 세션 2개를 제외 목록에 하드코딩**
태그가 생기기 **전에** 찍힌 행이라 소급 적용이 안 된다. 09:13 세션은 라운드1 utm이고
사람일 가능성을 배제 못 해서 **일부러 남겼다** (모양은 같지만 증거가 부족하다 —
없는 증거로 데이터를 지우지 않는다).

### 검증 (헤드리스 Chromium, 틱톡 인앱 UA / 390×664) — **22/22 PASS**
검증 트래픽은 `dk_events` 요청을 라우트에서 가로채 **실제 테이블에 한 건도 보내지 않았다**
(이번 루프는 오염을 0으로 만들면서 검증했다).

- A 자동화 로드: `person_id = PREVIEW-QA-anon` → 판독기가 세션 통째 제외 ✅
- B **실사용자 시뮬(webdriver=false)**: `person_id = anon` — **제외되지 않음**(가장 중요한 반대 방향 확인),
  `page_view`가 여전히 첫 추적 호출, meta `{did,repeat,store}` 정상, 세션 id 1개·길이 16,
  `scene_play` → 오디오 실제 진행(currentTime 1.04) → `scene_audio_ok` 발화, JS 에러 0
- C `?dk_qa=1` 명시(webdriver 위장 false): 정상 태그 ✅
- D 오퍼 무결성: $49 링크·`checkout_click{price:49}`·`track()`→dk_events 생존, `DK_PID` 정의 1개

**배포 확인(라이브 실측):** Pages 반영 후 라이브 HTML 직접 fetch(200, 699,216B) →
**로컬과 md5 동일** (`36e0cb0b…`), `track()`·dk_events 적재 코드·$49 링크 생존 확인.

**수정 후 판독기 재실행: `6 raw → 0 after exclusions (2 dropped)`.
루프 6 이후 실사용 세션은 정직하게 0이다.**

### 다음 루프 제안
1. **`python3 tools/dk_read.py --spend` 먼저.** $30 초과면 `1872041858734178` 즉시 정지.
2. **심사 상태 확인이 이번 국면의 유일한 게이트.** `ADGROUP_STATUS_AUDIT` →
   `ADGROUP_STATUS_DELIVERY_OK`로 바뀌었는지. 틱톡 심사는 보통 1~2시간, 최대 24시간이다.
   - **아직 AUDIT이고 생성 후 6시간이 넘었으면 그건 이상 신호다** — 반려 사유
     (`/adgroup/get/`의 상태·거절 필드) 확인하고, 그때는 사용자에게 알릴 것.
   - 스케줄 종료가 **07-30 04:43(계정 tz) = 09:43 UTC**다. 심사가 오래 끌면 집행 창이
     통째로 날아간다 — 그 경우 스케줄 연장이 다음 루프의 액션.
3. 집행 시작됐으면 `python3 tools/dk_read.py --since <집행 시작시각>`.
   이제 **QA 로드가 자동 제외되므로 남는 세션은 전부 진짜다.** 판독 순서(루프 6 기준 유지):
   - `perf`/`first_tap`이 page_view 대비 정상 비율인가 → 페이지가 실제로 뜨는가
   - `scene_play` → `scene_audio_ok` 비율 낮으면 인앱 오디오 차단 → 무음 자막 우선 모드
   - `leave.sec` 3초 미만 집중이면 훅 불일치 (`wall`과 혼동 금지)
   - `offer_view` 도달 후 `checkout_click` 0 (n≥20)이면 그때 $29 테스트
4. **앞으로 라이브 페이지를 헤드리스로 검증할 때는 `?dk_qa=1`을 붙이거나,
   이번 루프처럼 `dk_events` 요청을 라우트로 가로채 실제 적재를 막을 것.** 둘 다 하면 가장 안전.
5. 규율 유지: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것.

---

## 2026-07-29 09:45 UTC — 루프 6

**🟢 egress 열렸다 (6루프 만에). Supabase·TikTok·GitHub Pages 전부 200.
그리고 6루프 동안 "scene_play 0"으로 우리를 괴롭히던 미스터리가 풀렸다 —
그 0은 고쳐진 페이지의 성적표가 아니었다.**

### 데이터 스냅샷 (실측, 처음으로)
- 이벤트 총 1000행(RPC 상한), 2026-07-22 ~ 07-29 09:44 UTC
- 테스트 세션 1개(`mrw64tp7v313fh`) 제외 → 실사용 **499세션**
- **페이지가 둘 섞여 있었다**: `/learn-korean/episode1/` 287세션,
  루트 랜딩 `/learn-korean/` 212세션. 이벤트 어휘가 완전히 다르다
  (루트는 price_view·cta_click·signup_success·scroll_25~90)
- episode1 퍼널: page_view 287 → scene_play **0** → 이하 전부 0
- 루트 랜딩: page_view 211 → scroll_25 41(19%) → price_view 72(34%)
  → cta_click 2 → **signup_success 1**
- 틱톡 실지출: 07-28 **$28.60** / 클릭 304 / CPC $0.09 / CTR 5.14%,
  **07-29(오늘) $0.00** → 오늘 $30 한도 전액 여유

### 🔑 핵심 발견 — "scene_play 0"은 고친 페이지의 데이터가 아니다
episode1 299 page_view 중 **298개가 수정 이전 빌드**였다. 근거 두 개가 일치한다:
- `session_id` 길이 **14** (구 빌드 `slice(2,8)`), 현행 빌드는 **16**
- `page_view.meta`가 **null** — 현행 빌드는 항상 `{did,repeat,store}`를 보낸다

그리고 시간축이 이걸 확정한다:

| | |
|---|---|
| 구 빌드 트래픽 | 07-28 190건 + 07-29 00~06:26 UTC 108건 |
| 수정 배포 | 07:12 UTC(세션id) · 08:10 UTC(leave 비콘) |
| 수정 후 실사용 세션 | **1개** (09:13 UTC, len 16, meta 정상) |

즉 **고쳐진 페이지는 아직 사실상 한 명도 보지 못했다.** 루프 1~5가 축적한
"249~287명이 전원 재생을 안 눌렀다"는 공포는 **깨진 빌드의 기록**이었다.
`perf` 5건/`first_tap` 0건도 같은 이유 — 구 빌드엔 그 계측 자체가 없었다.

### 라이브 페이지는 실제로 동작한다 (실측으로 확인)
- 라이브 HTML 직접 fetch(200, 698,524B) → **로컬 HEAD와 md5 동일**
  (`6f53baa5…`) — 배포 확인. track/dk_events 적재 코드 생존 확인
- 헤드리스 Chromium + 틱톡 웹뷰 UA(390×664)로 실제 재생까지:
  `page_view → perf{kb:682,load:154} → first_tap → scene_play → scene_audio_ok{ms:3821}`
  전부 발화, **오디오 실제 재생(paused:false, currentTime 6.8s)**, pageerror 0
- 재생 버튼 박스 y=317~369 → 폴드 안 (루프 5 결론 재확인)

### 결정과 근거 → **백로그 2) 라운드2 점화**
막고 있던 조건이 전부 해소됐다: 페이지 정상 확인 ✅ 배포 확인 ✅ 오늘 지출 $0 ✅
그리고 "판을 다시 깔아야 한다"는 근거가 하나 더 나왔다 — **라운드1은 Pangle이
켜져 있었다.** 구 광고그룹은 `PLACEMENT_TYPE_AUTOMATIC`
(`PLACEMENT_TIKTOK` + `GLOBAL_APP_BUNDLE` + **`PANGLE`**), 연령·관심사 타겟 **없음**.
CTR 5.14%에 CPC $0.09짜리 클릭 304개의 정체가 이걸로 설명된다.
깨진 페이지 × Pangle 트래픽 = 라운드1 데이터는 두 겹으로 무효다.

**이번 루프의 의미 있는 변수 변경 1개 = 트래픽 재점화.** 가격·훅·카피는 손대지 않았다
(오퍼 도달 0이므로 가격 실험은 아직 근거 없음).

### 실행 내용
**1) 새 광고그룹 `1872041858734178` — DramaKorean_Ep1_US_r2_ttonly**
(기존 캠페인 `1871949785120465` **안에** 생성. 새 캠페인 만들지 않음. GlowGate 무접촉.)

| 항목 | 값 |
|---|---|
| 지면 | `PLACEMENT_TYPE_NORMAL` / **`PLACEMENT_TIKTOK` 단독 (Pangle 제외)** |
| 지역·연령 | 미국(6252001) / AGE_25_34·35_44·45_54 |
| 관심사 | K-Dramas, Korean drama, korean dramas, learn Korean, language learning |
| 예산 | **$30/일** (BUDGET_MODE_DAY), CPC/CLICK, smooth pacing |
| 스케줄 | 07-29 04:53 → 07-30 04:43 (계정 tz UTC-5) — 24h 안전 종료 |

**2) 광고 `1872041900786353` — dk_ep1_captioned_v1_r2**
기존 영상·아이덴티티·카피 그대로 재사용(변수 고정). 랜딩 URL만
`utm_campaign=dramakorean_ep1_r2`로 분리 → 라운드2를 구 트래픽과 섞이지 않게 읽는다.
현재 둘 다 `ENABLE` / 심사중(AUDIT).

**3) `tools/dk_read.py` 두 가지 판독 결함 수정** — 이번 루프가 직접 당한 것들이다
- **RPC 1000행 상한이 조용하다.** 첫 호출이 그대로 잘린 창을 돌려줬고, 나는
  거기서 "499세션"을 읽었다. → `since`를 앞으로 밀며 페이징 + 창 구간 출력
- **두 페이지를 한 퍼널에 섞고 있었다.** 루트+episode1을 합쳐 "page_view 498 →
  scene_play 0"으로 찍혀서, 루트에서 멀쩡히 일어나던 34% price_view와
  signup_success 1건이 통째로 안 보였다. → `--path`로 페이지 단위 스코프(기본 episode1),
  페이지별 page_view 분포 출력
- 추가: `meta:null` page_view = 구 빌드 트래픽이므로 **경고로 표면화**
  (다음 루프가 같은 착시에 빠지지 않게)

### 다음 루프 제안
1. **먼저 `python3 tools/dk_read.py --spend`.** $30 초과면 `1872041858734178` 즉시 정지.
2. 심사 통과·집행 시작 여부 확인(`ADGROUP_STATUS_AUDIT` → DELIVERY_OK).
   심사에서 막혔으면 그게 이번 루프의 유일한 블로커다.
3. `python3 tools/dk_read.py --since 2026-07-29T09:45:00Z` 로 **라운드2만** 판독.
   `utm_campaign=dramakorean_ep1_r2`만 진짜 신규다. 판독 순서:
   - `perf`/`first_tap`이 page_view 대비 정상 비율로 오는가 → 페이지가 실제로 뜨는가
   - `scene_play` → `scene_audio_ok` 비율 낮으면 무음/자막 우선 모드
   - `leave.sec` 3초 미만 집중이면 훅 불일치
   - `offer_view` 도달 후 `checkout_click` 0(n≥20)이면 그때 $29 테스트
4. 규율: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것. 라운드2 데이터가
   **처음으로 고쳐진 페이지의 데이터**다 — 오염시키지 말고 그대로 받을 것.

---

## 2026-07-29 09:10 UTC — 루프 5

**⚠ egress 차단 5연속. 이제 이건 이 루프의 유일한 블로커다 — 계측·성능·버그 위생은
전부 끝났고, 라운드2를 켜지 못하게 막는 건 네트워크 정책 하나뿐이다.**

### 네트워크 상태 (변화 없음)
- `pjvjweurelmosugwptdl.supabase.co` → CONNECT 403 (퍼널 판독 불가)
- `business-api.tiktok.com` → CONNECT 403 (지출 확인·라운드2 점화 불가)
- 프록시 `/__agentproxy/status`의 `recentRelayFailures`가 두 호스트 모두
  `connect_rejected: gateway answered 403` 로 기록 — TLS 아님, 게이트웨이 정책 거부
- GitHub MCP·git은 정상

### 데이터 스냅샷
새 실사용 세션 **0** (6루프째). 베이스라인 그대로: 지출 $24.29 / 클릭 266 /
랜딩 249세션 / scene_play 0 / 이하 전 단계 0.
집행 중 광고그룹 없음(1871949778453250 스케줄 종료) → 오늘 지출 $0, $30/일 한도
초과 위험 없음. (API 확인 불가, 근거는 스케줄 종료 사실.)

### 배포 상태 — 확인됨
GitHub MCP로 Actions 조회: `pages build and deployment` = **4939e81에 대해
08:10:37Z success**. 로컬 HEAD와 일치 → 루프 1~4의 수정(계측 head 이동, 3.3MB→182KB
압축, 오디오 복구, 세션 id, leave 비콘) 전부 라이브 서빙 중.

### 결정과 근거
신규 세션 0 → 규율상 큰 변수(가격·타겟·훅) 변경 금지. 백로그 1~3 완료, 4(점화)는
API 차단으로 계속 보류. 그래서 이번 루프는 **(a) 남은 퍼널 가설 하나를 실측으로 소거**하고
**(b) egress가 열리는 순간 판독이 즉시·정확하게 되도록 도구를 준비**하는 데 썼다.
페이지 코드는 한 줄도 건드리지 않았다.

#### (a) 소거된 가설: "재생 버튼이 인앱 웹뷰에서 폴드 아래 있다"
지금까지 모든 루프가 390×844(풀스크린 사파리)로만 검증했다. 그건 틱톡 웹뷰가 주는
높이가 **아니다** — 상·하단 크롬을 빼면 훨씬 짧다. 249명이 전부 로드해놓고 단 한 명도
재생을 안 눌렀다는 사실의 유력 용의자였다. 실측(헤드리스 Chromium, 틱톡 UA):

| 뷰포트 | 재생 버튼 top/bottom | 폴드 안? |
|---|---|---|
| 390×664 (iPhone14 웹뷰) | 317 / 369 | ✅ |
| 375×553 (SE 웹뷰) | 306 / 358 | ✅ |
| 360×520 (소형 안드로이드 웹뷰) | 295 / 347 | ✅ |
| 430×745 (ProMax 웹뷰) | 346 / 398 | ✅ |

**전부 폴드 안. 가설 기각.** `#player`가 `aspect-ratio:9/13`이라 뷰포트가 짧아지면
플레이어도 같이 줄어들어 버튼이 항상 상단 절반에 남는다. 다음 루프는 이 가설에
시간 쓰지 말 것.

추가로 TDZ 함정도 점검: `SUPABASE_URL`(6행) → `track`(18행) → `page_view`(19행) 순서라
조기 page_view가 상수보다 먼저 실행돼 catch에 삼켜지는 일은 없다. 계측 건전함 확인.

### (b) 실행 내용 — `tools/dk_read.py` 신설 (페이지 무변경, 커밋 1개)
매 루프 손으로 다시 짜던 판독을 스크립트로 고정했다. 제외 규칙이 미묘해서 손으로 하면
틀리고, 틀리면 **실제 돈 쓰는 결정이 틀린다.**

- 제외 규칙 4종 구현: 테스트 세션 `mrw64tp7v313fh` / `PREVIEW`·`ANCHTEST` person_id 포함
  세션 / anon 외 person_id 2개 이상 섞인 세션
- `leave`는 **세션당 seq 최대 행만** 채택 (루프 4의 last-beacon-wins 규칙)
- 퍼널 7단계 + 단계별 전환율, `store:0` 비율, `scene_play→scene_audio_ok` 비율,
  체류 median/p90/3초미만 비율, `offer_view→checkout_click`, utm 소스별 분해
- 루프 3·4가 세운 판독 기준을 **경고문으로 내장**: 오디오 비율 낮으면 무음 모드 검토,
  3초 미만 과다면 훅 문제, offer 도달 후 클릭 0(n≥20)이면 $29 테스트 정당,
  `wall≫sec`면 앱 전환이지 이탈 아님(혼동 방지)
- `--spend`: TikTok 오늘 지출 read-only 확인(계정 tz UTC-5 반영, 캠페인 1871949785120465
  합산, $30 초과 시 경고). **광고 생성·수정 기능은 일부러 넣지 않았다** — 점화는 다음
  루프에서 의도적으로 수동 실행

**검증: 합성 픽스처로 24행 → 16행, 4세션 정확히 제외 (제외 규칙 4종 전부 발화).**
seq 덕분에 S1 체류가 5초(seq1)가 아닌 95초(seq2)로 읽혔다. 특히 **테스트 세션의
`checkout_click`이 정확히 걸러졌다** — 손으로 읽었으면 "결제 발생!" 오탐이 났을 자리다.

### 다음 루프 제안
1. **egress부터 확인.** 열렸으면 코드 손대지 말고 순서대로:
   ① `python3 tools/dk_read.py --spend` (>$30이면 광고그룹 즉시 정지)
   ② 백로그 4) 라운드2 점화 — 기존 캠페인 1871949785120465 **안에** 새 광고그룹:
      미국 / 25-54 / 관심사 K-drama·Korean culture·language learning /
      틱톡 지면만(Pangle 제외) / $30 일예산, 광고 1871949837237713 재사용
   ③ 이후 `python3 tools/dk_read.py --since <점화시각>`
2. 여전히 막혀 있으면: **코드 위생 작업은 이제 수익이 0에 수렴한다.** 계측 3대 오염원
   제거 완료, 폴드 가설 기각, TDZ 없음, 배포 검증됨. 더 만질 곳을 찾는 것보다
   무행동 관찰이 낫다. (억지 변경은 규율 위반이자 라운드2 판독 오염원이 된다.)
3. 규율 유지: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것.

---

## 2026-07-29 08:15 UTC — 루프 4

**⚠ egress 차단 4연속 (Supabase·TikTok). 단, 이번 루프에 GitHub MCP 경로가 열려
3루프 밀려 있던 "배포 검증"을 드디어 해결했다.**

### 네트워크 상태
- `pjvjweurelmosugwptdl.supabase.co` → CONNECT 403 (퍼널 판독 여전히 불가)
- `business-api.tiktok.com` → CONNECT 403 (지출 확인·라운드2 점화 여전히 불가)
- `next-epic-dev.github.io` → CONNECT 403 (HTML 직접 fetch는 여전히 불가)
- 프록시 status로 재확인: TLS 문제 아님, 게이트웨이 정책 거부(`connect_rejected`)
- **신규: GitHub MCP 서버는 동작한다** → 저장소·Actions 조회 가능

### ✅ 3루프 밀린 항목 해소 — 배포 확인됨
GitHub MCP로 확인:
- master HEAD = `9effa0c` (로컬과 일치)
- `pages build and deployment` 워크플로 = **9effa0c에 대해 07:12:07Z success**
- 루프 1·2·3의 수정(계측 head 이동, 3.3MB→182KB 압축, 오디오 복구, 세션 id 수정)이
  **전부 라이브에 반영돼 있음이 확정**. 그동안 "미검증"으로 달아둔 꼬리표를 뗀다.
- 라이브 HTML 바이트를 직접 받아보는 것만 여전히 불가하나, 배포 커밋 SHA가 일치하고
  로컬 실측으로 track·dk_events 생존을 확인했으므로 실질 검증은 충족.

### 데이터 스냅샷
새 실사용 데이터 **0** (5루프째). 베이스라인 그대로: 지출 $24.29 / 클릭 266 /
랜딩 249세션 / scene_play 0 / 이하 전 단계 0.
집행 중 광고그룹 없음(1871949778453250 스케줄 종료) → 오늘 지출 $0, $30/일 한도
초과 위험 없음. (API 확인은 불가, 상태 근거는 스케줄 종료 사실.)

### 결정과 근거
신규 세션 0 → 규율상 큰 변수(가격·타겟·훅) 변경 금지. 백로그 1~3 완료, 4(점화)는
API 차단으로 계속 보류. 그래서 루프 3과 같은 방침으로 **라운드2 데이터를 신뢰할 수
있게 만드는 계측 감사**를 계속했고, 또 하나 치명적인 판독 오류를 찾았다.

`dkLeave()`가 **첫 번째** `visibilitychange→hidden`에서 latch(`DK_LEFT=1`)되고 이후
영구히 침묵했다. 우리 트래픽은 전량 틱톡·IG 인앱 웹뷰이고, 웹뷰는 앱 전환·알림 등으로
hidden을 수시로 발화한다. 결과:

1. **체류시간 과소 집계** — `sec`가 "페이지 체류"가 아니라 "첫 백그라운드까지의 시간"을
   재고 있었다.
2. **상태 동결** — `max_scroll`·`played`·`stage`가 그 이른 시점에 얼어붙는다. 3초에
   앱 전환했다가 돌아와서 장면을 재생하고 오퍼까지 스크롤한 사람이 **`sec:3`,
   `played:0`, `max_scroll:0`의 완벽한 이탈자**로 기록된다.

이게 왜 지금 고쳐야 하냐면 — 루프 3이 세워둔 라운드2 판독 기준에 **`leave.sec`가 3초
미만에 몰리면 로드/기대 불일치**가 있다. 즉 이 버그를 안 고치고 점화하면 판독이
"전원 3초 이탈"로 나오고, 다음 루프가 **멀쩡한 퍼널을 훅 문제로 오진**한다. 루프 3의
세션 id 붕괴와 정확히 같은 부류(점화 전에 반드시 고쳐야 하는 계측 버그)다.
이번 루프의 유일한 변수 변경.

### 실행 내용 (커밋 1개 `acc769e`, 되돌리기 쉬움)
- **last-beacon-wins로 전환**: hidden마다, 그리고 pagehide에 beacon 발화, 각각
  증가하는 `seq` 부여. **판독 규칙: 세션당 seq 최대인 행을 취한다.**
  (hidden마다 쏘는 이유 = 백그라운드 상태에서 웹뷰가 죽어도 최신 상태가 이미 적재됨)
- `sec` = **보이는 시간만** 누적(백그라운드 시간 제외), `wall` 신설 = 로드 후 실제 경과
- `why` 신설 = `hide` / `pagehide` 구분
- 카피·가격·타겟·재생 로직 변경 없음. 순수 계측 수리.

### 검증 (헤드리스 모바일 Chromium, 틱톡 인앱 UA / 390×844) — **26/26 PASS**
- A 백그라운드→복귀→참여: beacon 2개, seq [1,2] 증가, `sec:3` / `wall:4`
  → 백그라운드 1.2초가 실제로 차감됨
- B **상태 신선도(이번 수정의 핵심)**: 한 방문 안에서 `max_scroll 0→100`,
  `played 0→1`로 갱신. **구 코드였다면 이 방문이 스크롤 0·재생 0의 이탈로 기록됐다.**
- C 백그라운드 없이 종료: beacon 정확히 1개, `wall≈sec`, `why:pagehide`
- D 회귀: `track()`→dk_events 생존, `page_view`가 여전히 첫 추적 호출, 세션 id 1개로
  통일, $49 스트라이프 링크·`checkout_click{price:49}` 그대로, JS 에러 0

### 다음 루프 제안
1. **egress부터 확인.** Supabase·TikTok이 열렸는지. 막혀 있으면 광고·판독은 계속 보류.
   이건 사용자가 환경 네트워크 정책에 두 호스트를 허용해야 풀린다.
2. 뚫리면 즉시 순서대로: ① TikTok 오늘 지출 확인(>$30이면 광고그룹 정지)
   ② 백로그 4) 라운드2 점화 — 새 광고그룹(미국 / 25-54 / K-drama·Korean culture·
   language learning / 틱톡 지면만, Pangle 제외 / $30 일예산), 광고 1871949837237713 재사용
3. **라운드2 판독 기준 (갱신)**:
   - `leave`는 **세션당 seq 최대 행만** 사용할 것 (중복 행 정상)
   - 체류는 `sec`(참여시간), 이탈 속도 판단도 `sec` 기준. `wall`과의 차이가 크면
     앱 전환이 잦다는 뜻이지 이탈이 아니다 — **혼동 금지**
   - `page_view` meta `store:0` 비율 높으면 → 웹뷰 스토리지 제약 심함
   - `scene_play`는 있는데 `scene_audio_ok` 적으면 → 인앱 웹뷰 오디오 차단이 진범
     (무음 자막 우선 모드 검토)
   - `offer_view`는 나오는데 `checkout_click` 0이면 → 그때 가격 $29 테스트
4. 계측 위생은 이제 정말 다 됐다고 본다(세션 id·오디오·체류/스크롤 3대 오염원 제거).
   egress만 열리면 다음 루프는 코드 손대지 말고 바로 광고 점화.
5. 규율 유지: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것.

---

## 2026-07-29 07:10 UTC — 루프 3

**⚠ 데이터 판독 3연속 실패: egress 차단 지속. 이제 이건 "대기"가 아니라 사용자 조치가
필요한 블로커다 — 루프의 본업(데이터 판독·광고 운영) 전체가 3회 연속 정지 상태.**

이번엔 프록시 외 경로(WebFetch 툴)도 시도했으나 동일하게 403 — 툴 문제가 아니라
환경 네트워크 정책 차원의 거부임이 확정됐다.

- `pjvjweurelmosugwptdl.supabase.co` → CONNECT 403 (퍼널 판독 불가)
- `business-api.tiktok.com` → CONNECT 403 (지출 확인·라운드2 점화 불가)
- `next-epic-dev.github.io` → CONNECT 403 (curl·WebFetch 모두 실패, 라이브 검증 불가)
- git(origin)만 로컬 프록시로 정상 → 커밋·푸시는 가능

### 데이터 스냅샷
새 데이터 없음(4루프 연속). 베이스라인 그대로: 지출 $24.29 / 클릭 266 / 랜딩 249세션 /
scene_play 0 / 이하 전 단계 0. 광고그룹 1871949778453250은 스케줄 종료 = 집행 중 광고 없음
→ $30/일 초과 위험 없음(API 확인은 여전히 불가).

### 결정과 근거
데이터 0·신규 세션 0 → 규율상 큰 변수(가격·타겟·훅) 변경 금지. 백로그 1~3은 완료,
4(라운드2 점화)는 API 차단으로 계속 보류. 그래서 이번 루프는 **라운드2가 켜졌을 때
데이터를 신뢰할 수 있게 만드는 계측 감사**를 택했고, 실제로 치명적인 버그를 찾았다.

`DK_SID`가 `localStorage`에서 나오고 있었다. 두 가지가 동시에 깨진다:

1. **세션 붕괴** — 인앱 웹뷰(틱톡·IG)나 프라이빗 모드에서 `localStorage` 접근이 예외를
   던지면 catch가 **리터럴 `'nostore'`를 반환**했다. 즉 스토리지가 막힌 방문자 전원이
   *같은 session_id 하나*로 합쳐진다. 우리 트래픽은 전량 틱톡 인앱 웹뷰다. 라운드2를
   그대로 켰으면 그 사람들의 퍼널이 통째로 세션 1개로 집계되고, 심지어 "한 세션에
   person_id 2개 이상 섞이면 제외" 규칙에 걸려 **분석에서 통으로 삭제**됐을 것이다.
2. **방문 병합** — 스토리지가 되는 경우엔 `dk_sid`가 영구 보존돼 재방문이 같은 세션으로
   합쳐진다. 라운드1 방문자가 라운드2에 다시 오면 라운드1 id를 그대로 들고 온다.

즉 라운드2를 점화하기 **전에** 고쳐야 하는 버그였다. 데이터가 없는 지금이 정확히 이걸
고칠 타이밍이고, 이게 이번 루프의 유일한 변수 변경이다.

### 실행 내용 (커밋 1개 `39ab212`, 되돌리기 쉬움)
- `DK_SID` = **페이지 로드마다 새로 생성**, 스토리지 의존 완전 제거 (1세션 = 1방문)
- `DK_DEV` 신설 — 기기 id는 best-effort로만 저장, 실패해도 세션 id에 영향 없음
- `page_view` meta에 `{did, repeat, store}` 추가.
  **`store`가 이번 라운드의 새 관측치**: 틱톡 인앱 웹뷰 중 스토리지가 막힌 비율을
  처음으로 측정하게 된다(웹뷰 제약이 얼마나 심한지 알려주는 대리 지표).
- 카피·가격·타겟·재생 로직 변경 없음. 순수 계측 수리.

### 검증 (헤드리스 모바일 Chromium, 틱톡 인앱 UA / 390×844) — **23/23 PASS**
- A 정상 스토리지: `store:1`, `repeat:0`, did 존재, JS 에러 0
- B 같은 브라우저 재방문: **session_id는 새로 발급**, did는 동일, `repeat:1`
- C **스토리지 차단 웹뷰**: 독립 방문자 2명이 **서로 다른 session_id** 획득(핵심 수정),
  `'nostore'` 소멸, `store:0`으로 차단 사실이 기록됨, JS 에러 0
- D 차단 웹뷰에서 전체 퍼널 회귀: 오디오 실제 진행(currentTime>0.1),
  `scene_play`·`scene_audio_ok` 발화, **한 방문의 모든 이벤트가 session_id 1개 공유**
- E 페이지 무결성: `track()`→dk_events 생존, `page_view`가 여전히 첫 추적 호출,
  $49 스트라이프 링크·`checkout_click{price:49}` 그대로

전체 퍼널 별도 확인: `page_view → perf → first_tap → scene_play → scene_audio_ok →
scene_complete → scroll → quiz_done → teaser_view → offer_view`, 세션 1개, JS 에러 0.

**미검증:** 라이브 HTML 확인은 egress 차단으로 3루프째 불가(로컬 실측으로 대체).
집행 중 광고가 없어 이 배포로 깨질 라이브 트래픽은 없음.

### 다음 루프 제안
1. **egress부터 확인.** 여전히 막혀 있으면 광고·데이터는 전부 보류. 이건 사용자가
   환경 네트워크 정책에 위 3개 호스트를 허용해줘야 풀린다 — 이번 루프에 알림 발송함.
2. 뚫리면 즉시: ① TikTok 오늘 지출 확인(>$30이면 광고그룹 정지) ② 라이브 HTML에서
   track·dk_events 생존 확인(3루프 밀린 항목) ③ 그 다음 백로그 4) 라운드2 점화 —
   새 광고그룹(미국 / 25-54 / K-drama·Korean culture·language learning / 틱톡 지면만,
   Pangle 제외 / $30 일예산), 광고 1871949837237713 재사용
3. 판독 기준선(라운드2 데이터가 들어오면):
   - `page_view` meta `store:0` 비율이 높으면 → 웹뷰 제약이 심하다는 신호
   - `scene_play`는 있는데 `scene_audio_ok`가 적으면 → 인앱 웹뷰 오디오 차단이 진범
     (무음 자막 우선 모드 검토)
   - `leave.sec`가 3초 미만에 몰리면 → 로드/기대 불일치
   - `offer_view`는 나오는데 `checkout_click` 0이면 → 그때 가격 $29 테스트
4. 계측 위생은 이제 다 됐다고 본다. egress만 열리면 다음 루프는 바로 광고 점화.
5. 규율 유지: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것.

---

## 2026-07-29 06:11 UTC — 루프 2

**⚠ 데이터 판독 2연속 실패: egress 차단 지속 (조직 정책 403, 우회 불가).**

프록시 상태 엔드포인트(`/__agentproxy/status`)로 원인 확정 — TLS/설정 문제가 아니라
게이트웨이가 CONNECT에 403으로 답하는 **정책 거부**. README 지침상 재시도·우회 금지 대상.

- `pjvjweurelmosugwptdl.supabase.co` → 403 (퍼널 판독 불가)
- `business-api.tiktok.com` → 403 (지출 확인·광고그룹 생성 불가)
- `next-epic-dev.github.io` → 403 (라이브 HTML 검증 불가)
- git(origin)은 로컬 프록시로 정상

### 데이터 스냅샷
새 데이터 없음(3루프 연속). 베이스라인 그대로: 지출 $24.29 / 클릭 266 / 랜딩 249세션 /
scene_play 0. 광고그룹 1871949778453250은 스케줄 종료 상태 = **현재 집행 중 광고 없음**
→ $30/일 한도 초과 위험 없음(단, API로 확인은 못 했으므로 다음 루프 최우선 재확인).

### 배포 상태 확인 (루프 1 미검증 항목 해소)
루프 시작 시 HEAD가 detached였고 로컬 `origin/master` ref가 b2c8560에 멈춰 있어
"루프 1이 푸시 안 됨"으로 보였다. `git fetch` 후 실제 원격 확인 결과 **루프 1의 3개
커밋은 정상 푸시돼 있었다**(원격 master = 1313792). 로컬 ref만 낡았던 것.
0.69MB 페이로드 + head 최상단 계측은 이미 라이브에 반영된 상태.

### 결정과 근거
데이터 0·신규 세션 0이므로 규율상 큰 변수(가격·타겟·훅) 변경 금지.
광고 점화(백로그 4)는 API 차단으로 계속 보류. → **백로그 3) 재생/오디오 코드 검토**를 실행.

코드를 읽어보니 실제 버그가 있었다. `startPlay()`는 `au.play()`를 호출하기 **전에**
커버와 재생 버튼을 이미 숨긴다. 그래서 인앱 웹뷰(틱톡·인스타)가 자동재생 정책으로
`play()`를 reject하면 사용자는 **소리도 안 나고 다시 누를 버튼도 없는 죽은 화면**에 갇힌다.
게다가 `scene_play`는 성공 여부와 무관하게 찍혀서, 재생 실패가 성공으로 기록된다.
우리 트래픽 전량이 틱톡 인앱 웹뷰라는 걸 감안하면 이건 퍼널 최상단의 치명적 실패 모드.

### 실행 내용 (커밋 1개 `1f90d5c`, 되돌리기 쉬움)
- `playFailed()` 신설 — 실패 시 커버·재생 버튼 복구 + 재시도 힌트 노출(두 번째 탭은 대개 성공)
- 2초 워치독 — `play()`가 resolve됐는데도 재생이 전혀 진행 안 되는 웹뷰 케이스 포착
- audio `error` 이벤트도 같은 복구 경로로 통합
- **신규 `scene_audio_ok` 이벤트** — 실제 재생 진행(currentTime>0.15) 시 1회.
  이제 `scene_play`(=탭했다)와 실제로 소리를 들었다를 구분 가능
- 진행바 `au.duration` NaN 가드

실패 경로에서만 동작이 바뀐다(현재 그 경로는 막다른 길). 카피·가격·타겟 변경 없음.

### 검증 (헤드리스 모바일 Chromium, 틱톡 인앱 UA / 390×844)
A) 정상 재생, B) `play()`를 강제 reject시킨 웹뷰 시뮬레이션 — **11개 체크 전부 PASS**:
- A: `scene_play` → `scene_audio_ok` 발화, 오디오 실제 진행, 복구 UI 미노출, JS 에러 0
- B: `play_error{NotAllowedError}` 발화, `scene_audio_ok` 미발화(정확), 커버·버튼·힌트 복구, JS 에러 0

전체 퍼널 회귀도 확인(내가 `timeupdate`를 건드렸으므로):
`page_view → perf → first_tap → scene_play → scene_audio_ok → scene_complete → scroll →
quiz_done → teaser_view → offer_view`, JS 에러 0, 결제 링크 $49 그대로 살아있음.

**미검증:** 라이브 HTML 재확인은 egress 차단으로 여전히 불가(로컬 파일 실측으로 대체).
집행 중 광고가 없어 이 배포로 깨질 라이브 트래픽은 없음.

### 다음 루프 제안
1. **egress부터 확인.** 계속 막혀 있으면 광고·데이터는 전부 보류. 사용자 조치 필요 —
   환경 네트워크 정책에 위 3개 호스트 허용이 있어야 이 루프가 본래 일을 할 수 있다.
2. 뚫리면 즉시: ① TikTok 오늘 지출 확인(>$30이면 광고그룹 정지) ② `perf`/`leave`/
   `first_tap`/`play_error`/`scene_audio_ok` 판독 — 루프1·2 배포 이후 데이터만 유효
3. 그 다음 백로그 4) 라운드2 점화: 새 광고그룹(미국 / 25-54 / K-drama·Korean culture·
   language learning / 틱톡 지면만, Pangle 제외 / $30 일예산), 광고 1871949837237713 재사용
4. 판독 기준선: `scene_play`는 있는데 `scene_audio_ok`가 적으면 → 인앱 웹뷰 오디오 차단이
   진범(그럼 무음 자막 우선 모드 검토). `leave`의 sec가 3초 미만에 몰리면 → 로드/기대 불일치.
   offer_view는 나오는데 checkout_click이 0이면 → 그때 가격 $29 테스트.
5. 규율 유지: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것.

---

## 2026-07-29 05:13 UTC — 루프 1

**⚠ 데이터 판독 실패: 이 세션의 외부 egress가 차단됨 (정책 403).**

- `pjvjweurelmosugwptdl.supabase.co` → CONNECT 403 (퍼널 데이터 읽기 불가)
- `business-api.tiktok.com` → CONNECT 403 (오늘 지출 확인 불가, 광고그룹 생성 불가)
- `next-epic-dev.github.io` → CONNECT 403 (라이브 HTML 검증 불가)
- git(origin)만 로컬 프록시로 통함 → 커밋·푸시는 가능

### 데이터 스냅샷
새 데이터 없음. 베이스라인(라운드1) 그대로 사용:
지출 $24.29 / 클릭 266 (CTR 5%, CPC $0.09) / 랜딩 249세션 / scene_play **0** / 이하 전 단계 0.

지출 확인은 불가했으나 **$30 한도 초과 위험 낮음** — 유일한 광고그룹 1871949778453250은
스케줄 종료 상태라 현재 집행 중인 광고가 없음. 다음 루프에서 최우선으로 재확인할 것.

### 결정과 근거
데이터를 못 읽고 새 실사용 세션도 0개이므로 규율상 큰 변수(가격·타겟·훅) 변경 금지.
백로그 1)·2) 위생 수리만 실행. 광고 라운드2 점화(백로그 4)는 API 차단으로 **보류**.

먼저 "scene_play가 정확히 0"이 사람 행동인지 버그인지부터 갈랐다.
헤드리스 모바일 Chromium(iPhone UA, 390×844)으로 실제 페이지를 돌려 확인:

- `startPlay`는 정상 함수, 커버 숨김 → 오디오 재생(18.68초) → 자막 싱크 → 6줄 렌더 정상
- 이벤트도 정상 발화: `page_view → scene_play → scene_complete → quiz_done → teaser_view → offer_view`
- JS 에러 0건, 오퍼의 스트라이프 링크 살아있음
- `SUPABASE_URL/KEY`는 68행 const 체인 끝에 정의돼 있어 track()도 정상

**결론: 페이지는 안 깨졌다.** scene_play 0의 진짜 용의자는 로드 이탈 + 계측 실명(失明):
`track('page_view')`가 3.87MB 중 약 97% 지점(라인 73)에 있어서, **다 받기 전에 나간 사람은
애초에 데이터에 안 찍힌다.** 즉 249 page_view는 "끝까지 받은 사람" 수이고, 그 앞단 이탈은
지금까지 통째로 안 보였다. 눈부터 뜨는 게 순서.

### 실행 내용 (커밋 2개, 되돌리기 쉬움)

1. `7a5405f` — 커버 이미지 인라인 PNG 압축
   - 원본은 768×1376인데 PNG로 3.36MB(base64)로 박혀 있었음 — 해상도 문제가 아니라 인코딩 낭비
   - 원본 해상도 유지, JPEG q=74 progressive → **182KB** (목표 200KB 이하 충족)
   - 어차피 어두운 그라디언트 오버레이 아래라 아티팩트 안 보임 (스크린샷 확인)
   - **페이지 총량 3.87MB → 0.69MB (-82%)**

2. `ac83d07` — 계측 수리
   - 추적 코어(SUPABASE 상수·DK_SID/PID/UTM·track·`page_view`)를 `<head>` 최상단으로 이동
     → 이제 3.3MB 페이로드 **이전에** page_view가 찍힘. 조기 이탈이 처음으로 보이게 됨
   - 신규 이벤트:
     - `perf` — {ttfb, dcl, load, kb} (Navigation Timing, load 시 1회)
     - `first_tap` — {ms, target} 첫 탭까지 걸린 시간
     - `scroll` — 10% 이상 첫 스크롤 시 {ms}
     - `leave` — pagehide/visibilitychange hidden 시 {sec 체류, max_scroll, played, stage}
     - `play_error` — `au.play()` promise reject + audio `error` 이벤트 (인앱브라우저 자동재생
       차단을 이걸로 잡는다 → 백로그 3의 절반 선반영)

### 검증
헤드리스 모바일 Chromium 실측 — 신·구 이벤트 전부 발화 확인, JS 에러 0:

```
page_view
perf {"ttfb":0,"dcl":32,"load":63,"kb":679}
first_tap {"ms":595,"target":"btn"}
scene_play
scene_complete
scroll {"ms":1555}
quiz_done {"correct":1,"total":3}
teaser_view
offer_view
leave {"sec":2,"max_scroll":100,"played":0,"stage":"stage-scene"}
```

**미검증 항목:** 라이브 HTML 재확인(배포 후 track 생존 확인)은 egress 차단으로 못 함.
로컬 파일 실행 검증으로 대체했고, Pages는 master 파일을 그대로 서빙하므로 위험은 낮다고 판단.
현재 집행 중인 광고가 없어 이 배포로 깨질 라이브 트래픽도 없음. **다음 루프에서 최우선 확인.**

### 다음 루프 제안
1. **egress 복구 여부부터 확인.** 막혀 있으면 광고·데이터 관련은 전부 보류하고 위생 수리만.
2. 뚫렸으면 즉시: ① TikTok `report/integrated/get`으로 오늘 지출 확인(> $30이면 광고그룹 정지)
   ② Supabase에서 `perf`/`leave`/`first_tap` 판독 — **이번 배포 이후 데이터만** 의미 있음
3. 그 다음 백로그 4) 라운드2 점화: 새 광고그룹(미국 / 25-54 / K-drama·Korean culture·language
   learning 관심사 / 틱톡 지면만, Pangle 제외 / $30 일예산), 광고 1871949837237713 재사용.
   0.69MB 페이지 + 조기 page_view가 깔린 상태라 이번엔 앞단 이탈이 처음으로 측정된다.
4. 판독 기준선: page_view 대비 scene_play 비율이 낮으면 → 첫 화면 훅 문제.
   `leave`의 sec가 3초 미만에 몰리면 → 여전히 로드/기대 불일치.
   offer_view는 나오는데 checkout_click이 0이면 → 그때 가격 $29 테스트.
5. 규율 유지: 새 실사용 세션 30개 미만이면 큰 변수 손대지 말 것.
